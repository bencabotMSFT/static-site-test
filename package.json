{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": { 
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "build": "echo 'Building the project...' && mkdir -p dist && cp -r * dist/"
  }
}