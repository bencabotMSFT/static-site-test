{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "build": "npx webpack --config webpack.config.js && npx mkdirp dist && npx copyfiles 'index.html' 'index.js' 'css/**/*' dist"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "dotenv-webpack": "^8.1.0",
    "webpack": "^5.0.0",
    "webpack-cli": "^4.0.0",
    "babel-loader": "^8.0.0",
    "mkdirp": "^1.0.4",
    "copyfiles": "^2.4.1"
  }
}
