{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "build": "mkdir -p dist && rsync -av --exclude='dist' ./ dist/"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
